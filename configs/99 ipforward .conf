# /etc/sysctl.d/99-ipforward.conf
# Enables IP forwarding on the router VM
# Required for Linux to route packets between interfaces
# Apply with: sudo sysctl -p /etc/sysctl.d/99-ipforward.conf

net.ipv4.ip_forward=1